# API Gateway Service
# 暴露到外部访问（端口 8080）
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: social-media-hub
spec:
  selector:
    app: api-gateway
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
  type: NodePort