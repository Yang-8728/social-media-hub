apiVersion: apps/v1
kind: Deployment
metadata:
  name: merger
  namespace: social-media-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: merger
  template:
    metadata:
      labels:
        app: merger
    spec:
      containers:
      - name: merger
        image: social-media-hub-merger:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: REDIS_URL
          value: "redis://redis:6379"
        volumeMounts:
        - name: videos
          mountPath: /app/videos
      volumes:
      - name: videos
        hostPath:
          path: /mnt/c/Code/social-media-hub/videos
          type: DirectoryOrCreate
